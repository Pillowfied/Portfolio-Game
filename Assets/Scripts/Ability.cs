using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ability : MonoBehaviour
{
    [SerializeField] int _duration;
    [SerializeField] int _damagePerTurn;
    [SerializeField] int _oneTimeDamage;
    [SerializeField] int _addArmor;
    [SerializeField] int _reduceArmor;
    [SerializeField] int _addStrength;
    [SerializeField] int _reduceStrength;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
